[project]
name = "jpl-horizons-mcp"
version = "0.1.0"
description = "Access the JPL Horizons tools through Model Context Protocol."
authors = [
    {name = "Ryan DeMuse"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.14.3",
    "fastmcp>=3.4.4",
    "geopy>=2.5.0",
    "httpx>=0.28.1",
    "mcp[cli]>=1.28.1",
    "pydantic>=2.13.4",
]
